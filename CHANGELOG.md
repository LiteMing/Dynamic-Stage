# Dynamic Stage 更新日志

本文记录 Dynamic Stage 自迁移至当前仓库以来的主要用户可见变更。项目采用
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的组织方式；日期为仓库中的版本整理日期。

## 1.5.0 - 2026-08-18

### 新增

- 新增可配置的全服关卡引导消息，播报实例的新建、重建、进入与退出；可通过 `config/dynamicstage-server.json` 或 `/dstage guide on|off|status` 控制。
- 新增 `/dstage gui` 普通玩家界面，可浏览运行中的关卡实例、直接加入，并调整边界显示、服务端 LOD 下载和实验性 Voxy 碰撞等纯客户端设置。
- 新增 `/dstage invite <玩家>`。接收方可点击聊天中的接受按钮加入邀请者所在实例，接受时会重新验证邀请有效期和人数上限。
- 玩家通过 `/tp` 等方式进入已有实例区域时，DS 会自动绑定该实例的 LOD、Flight、天空和边界；实例未满时作为成员加入，已满时以不占容量的观察者身份进入。

### 调整

- 玩家可见的实例标识统一为稳定的 `槽位序号(关卡名)`，例如 `3(gr1)`；`/dstage join` 的补全与参数使用该标识。
- 实例 UUID 不再作为普通玩家必须输入的标识，仅保留在 GUI 和邀请消息的悬浮诊断信息中；旧 UUID 参数仍可兼容解析。
- 满员观察者会保留进入关卡前的位置和游戏模式，离开关卡或 LOD 准备失败时自动恢复。
- 实例容量只统计正式成员，观察者仍会收到完整场景同步，但不会占用玩家名额。
- Editor 保存的便携模板改为可直接编辑的 `dynamicstage/templates/<id>.json`；原版场地方块和实体快照单独保存为内容寻址的 `dynamicstage/arenas/<sha256>.nbt`，相同快照自动复用。
- 命名 Flight 在模板 JSON 中保存全局 Flight 名称引用；未命名 Flight 以内嵌 JSON 保留。旧模板 `.dat` 和旧 `config/dynamicstage/templates` 按既定决策直接废弃，不再读取或迁移。
- `/dstage template delete` 只补全可删除的本地 JSON 模板；数据包模板会明确提示其为只读资源。

### 修复

- `/dstage flight play <name>` 不再被命令方块误判为缺少 `fade/blur` 转场参数；无附加参数时明确按即时切换执行。

## 1.4.0 - 2026-08-18

### 新增

- 新增只读 Voxy 缓存优化器。优化结果始终写入新的 LOD 包，不覆盖源缓存。
- 支持通过数据包 `data/<namespace>/dynamicstage/stages/*.json` 注册可分发关卡模板。
- 数据包模板可以声明相对实例原点的原版结构列表和玩家进入偏移，由 DS 统一负责实例初始化、重置与清理。
- 新增保留 ID `dynamicstage:none`，无 LOD 背景的关卡可跳过客户端缓存检查和挂载。
- 支持按 `min_y` / `max_y` 删除对关卡背景无意义的地下或高空数据。
- 支持以来源 LOD 锚点为中心，按 X/Z 圆形半径裁切背景范围。
- 支持保留指定深度的可见外壳，并合并远离空气、透明方块和液体的实体内部体素。
- 优化后从保留的第 0 级数据重新生成全部 Voxy mip 层，避免旧的远距离层重新带回已裁切内容。
- 编辑器新增实例管理页，可查看实例 UUID、模板、槽位、人数和生命周期，并可加入、保留或释放空实例。
- 关卡编辑模式移入编辑器实例页，可直接切换当前场地的编辑权限。
- 新增以下客户端命令：

```text
/dstage lod optimize voxy crop <source_pack> <output_pack> <min_y> <max_y>
/dstage lod optimize voxy shell <source_pack> <output_pack> <shell_depth> <min_y> <max_y>
/dstage lod optimize voxy radius <source_pack> <output_pack> <anchor_x> <anchor_z> <radius> <min_y> <max_y>
/dstage lod optimize voxy radius-shell <source_pack> <output_pack> <anchor_x> <anchor_z> <radius> <shell_depth> <min_y> <max_y>
```

### 调整

- 外层 `dynamicstage/templates` 中的管理员模板可覆盖同名数据包模板，编辑整合包关卡时无需修改原始数据包。
- 将容易误解的“重置策略”改为“实例生命周期”：新实例始终按模板构建；`无人时释放`会在最后成员离开后释放槽位，`保留实例`则持久保留场地和实例 UUID。
- 共享模板会优先加入同模板且未满的实例，只有没有可加入实例时才创建新实例；独立模板仍始终创建新实例。
- 旧模板中的 `ON_CREATE` / `MANUAL` 会分别迁移为 `RELEASE_WHEN_EMPTY` / `RETAIN`。
- 删除 `/dstage edit` 子命令，避免占用命令树；对应操作统一由编辑器实例页提供。

### 兼容性

- Voxy 缓存优化命令需要配套的 Voxy HDRS `0.0.4` 或更新版本；可配置关卡近裁切需要 HDRS `0.0.7` 或更新版本。

### 修复

- Voxy 优化包会在生成完成前同步刷新并压实 RocksDB，避免首次挂载前保留数十 MiB 的临时 WAL 写放大；命令报告的大小现在就是可直接分发的最终占用。
- 在运行中的实例重载其他模板时，会先清除旧模板留下的方块、非玩家实体和 Loquat 区域；空场地模板也会执行完整清场。
- 编辑器会以服务端返回的模板值初始化并确认保存结果，不再把活动关卡的实例生命周期误显示为默认值。
- `无人时释放`现在会清理整个 2048 格实例槽位内的非玩家实体和 Loquat 区域，并递归移除载具乘客；清场失败时保留槽位，避免残留内容污染后续实例。
- 使用当前原生 Voxy LOD 时会检查 RocksDB 是否实际包含地形区段；空缓存或关闭区块摄取时给出明确提示，不再生成无法渲染的关卡背景。
- 关卡 Voxy 渲染恢复经过验证的 `0.1` 近裁剪面，避免原生 `8/16` 近裁剪面丢失相机附近背景；普通世界 Voxy 仍使用原生投影。
- 配套 HDRS 删除了把近裁剪面内所有 LOD 顶点压到同一深度的错误补丁，避免中近距离出现共面闪烁、斜纹和突兀的层级差异。
- 编辑器新增 `Voxy 近裁剪`输入框，关卡可在 `0.01..16` 范围内独立保存投影 near plane，默认值为 `0.5`；运行中也可通过 `/dstage backdrop voxy-near <distance>` 调整。

## 1.3.2 - 2026-08-18

### 新增

- 新增受控的关卡编辑模式：创造模式玩家可通过 `/dstage edit on|off|status` 在关卡内建造和调整场景，退出编辑模式后恢复只读限制。
- 普通玩家现在可以使用 `/dstage join <instance_uuid>` 加入有空位的运行中实例，并可补全当前可加入的实例 UUID。
- 新增实验性 Voxy LOD 碰撞探测。客户端可检测玩家模型与只读 LOD 表面的交叠，并向服务端扩展事件报告方向与穿入距离。
- 编辑器本地设置页加入实验性 Voxy 碰撞事件开关，也可使用 `/dstage lod collision on|off|status` 调整。

### 调整

- 关卡管理和场景修改命令继续要求管理员权限；玩家自身的 `join`、`exit` 与状态查询不再被根命令权限一并拦截。
- 编辑能力仅在创造模式和当前关卡边界内生效，避免普通游玩或相邻实例被意外修改。
- 实验性 LOD 碰撞功能只报告事件，不接管玩家移动；观察者不会触发报告。

## 1.3.1 - 2026-08-18

### 新增

- 新增 `/dstage reload`，可在不重启服务端的情况下重新扫描模板、全局 Flight 和服务端 LOD 包，并刷新在线管理员的编辑器列表。
- 服务端可直接读取未打包的 `dynamicstage/lodpacks/<namespace>/<pack>/`，按需生成并缓存 `.dstlod`，不再要求管理员手工建立 HTTP 服务或预先导出归档。

### 调整

- 模板、LOD 包和 Flight 统一收纳到游戏或服务端实例根目录的 `dynamicstage/` 下，便于把完整关卡资源作为一个目录迁移和分发。
- 旧的 `config/dynamicstage/templates` 会自动迁移到 `dynamicstage/templates`。
- 服务器生成的归档和哈希放入 `dynamicstage/.cache`，与需要分发的源资源分离。

## 1.3.0 - 2026-08-15

### 新增

- 新增服务端 LOD 包分发协议。关卡可把背景包声明为 `optional` 或 `required`，客户端完成校验和安装后再进入关卡或切换背景。
- 支持通过 HTTP(S)/CDN 发布 `.dstlod`，并校验声明大小、SHA-256、ZIP 路径、条目数、解压大小和包清单。
- 支持服务端经 Minecraft 网络分块传输本地归档，适合直接分发小型和中型 Voxy 背景包。
- 客户端下载支持 `.part` 断点续传和原子安装。
- 新增客户端 LOD 下载开关和 `1..512 MiB` 大小上限，可在编辑器本地设置页或 `/dstage lod downloads` 中调整。
- 新增 `/dstage lod export`，可把自包含的 DH/Voxy 包导出为不可变 `.dstlod` 归档。

### 安全性

- 导出与安装会拒绝符号链接、目录逃逸和异常归档规模，并忽略 RocksDB 锁文件与诊断日志。
- 链接型 LOD 包不能导出，避免把本机绝对路径误当成可分发资源。

## 1.2.2 - 2026-08-15

### 新增

- 编辑器可在运行中的实例内选择并重载指定模板；实例 UUID、区域槽位、成员与返回点保持不变。
- 客户端设置加入边界可视距离、透明度和后备颜色，修改后即时生效。
- 全局 Flight 改为 `dynamicstage/<name>.json` 的可编辑 UTF-8 JSON 文件，可直接在游戏外维护并跨存档复用。

### 调整

- 场地快照改为稀疏结构：空气不再写入 NBT，空旷大关卡的模板体积主要随实际方块、方块实体和实体数量增长。
- 调整边界尺寸会清除尺寸不再兼容的旧快照，避免保存超大或错位结构。
- 服务端模板边界颜色优先；模板未指定颜色时才使用客户端后备色。

### 修复

- 玩家越过某一面边界后，该面会从外向内完整显示；其余仍在范围内的墙面继续按各自距离淡出。
- LOD 移动倍率只作用于玩家在关卡中的平移，不再放大眼高或第一/第三人称镜头偏移，修复 F5 切换视角时背景跳动和穿帮。

## 1.2.1 - 2026-08-15

### 新增

- 虚拟边界碰撞扩展到玩家以外的生物和一般实体；投射物仍可按设计穿过边界。
- 增加 Loquat 区域快照兼容，恢复模板前会清理目标位置的旧区域，避免重复边界报错。

### 修复

- 加强关卡外的边界可视化，便于离开场地的观察者重新定位关卡。

## 1.2.0 - 2026-08-15

### 新增

- 新增运行中背景调度：可在同一实例内反复切换 LOD 包、显示或隐藏背景，并选择立即、淡入淡出或模糊转场。
- 新增运行中 Flight 调度：可播放任意全局 Flight、使用模板绑定的 Flight 或停止动画，并与背景转场组合。
- 新增常驻 LOD 高斯模糊设置；滤镜只处理原生 LOD 后端的中间颜色纹理，不覆盖天空、场地方块、实体和 UI。
- 编辑器加入 Flight 名称选择、绑定管理和 Voxy 剔除控制。
- 新增 Voxy 相机邻近区块保留模式，用于阻止玩家跨越 Voxy LOD 分区时出现一帧剔除闪烁。

### 调整

- Voxy 关卡投影固定使用经过验证的 `0.1` 近裁剪面；`voxy-culling` 只控制相机附近 LOD 区块的原生剔除，不再错误地把它当作 near plane 倍率。
- Flight 使用共享的服务端游戏时间纪元，同一实例内的多个客户端会采样相同动画位置。
- 运行中调度保持命令级接口，方便 KubeJS 按音乐 marker、玩家 NBT 或计时事件组织多个 LOD 与 Flight，而不把音乐系统耦合进 DS。

## 1.1.2 - 2026-08-15

### 新增

- 新增可跨存档使用的 CMDCam Flight 库，并在编辑器中加入 Flight 选择、绑定与清除操作。
- 支持从 CMDCam 当前 SavedData、旧版 `cmdcam_Scenes.dat`、26.2 维度目录下的 `scenes.dat` 或明确指定的外部文件导入场景。
- 对单场景文件可自动选择；多场景文件会列出合法场景名。导入时接受 `default`/`outside`，并忽略对背景动画无意义的 `smooth_start` 限制。
- Flight 的 XYZ 驱动 LOD 和云层位移，yaw、pitch、roll、zoom 同时驱动 LOD 与所选天空；玩家仍可正常移动和控制自己的主镜头。

### 修复

- 客户端外观指令更新时不再短暂移除服务端边界碰撞，修复站在边缘的玩家在更新天空或颜色时穿过场地的问题。

## 1.1.1 - 2026-08-15

### 新增

- 补全关卡编辑器的简体中文和英文翻译键。
- Voxy 背景挂载失败时输出具体激活原因，便于区分缓存路径、世界 ID、存储锁和兼容版本问题。

### 兼容性

- 配套 Voxy HDRS 为跨版本无法反序列化的方块状态增加可见材质回退，并确保失败加载时释放世界存储锁；无法识别的新砖块系列回退为普通石砖而非岩石。

## 1.1.0 - 2026-08-14

### 新增

- 新增外部原生 LOD 缓存导入：`link` 原地挂载、`link-relative` 使用实例内可迁移相对路径、`copy` 建立独立副本。
- `/dstage start <stage>` 可自动检测当前世界唯一打开的 DH/Voxy 缓存，以玩家当前位置作为来源锚点并进入关卡。
- 缓存缺失不再阻止进入；客户端会给出警告并以无 LOD 背景模式继续。
- 新增玩家移动跟随开关和移动倍率。关闭后背景固定在来源锚点，开启后可按倍率映射有限场地中的玩家移动。
- 新增每关卡 DH 近景淡出倍率、天空模式和纯客户端时间模式：跟随主世界、固定时间或独立循环。
- 新增可移植关卡模板与编辑器。模板可保存实例模式、容量、重置策略、边界、场地快照、LOD 锚点、背景、Flight、天空和时间等参数。
- 支持 `parallel` 独立实例和 `shared` 共享实例；`capacity = 1` 可声明单人关卡。
- 新增本地边界外观配置和编辑器预加载/保存流程。

### 兼容性

- 增加 Forge DH + Embeddium + Oculus 开发运行配置，并修复 Oculus 下 DH 背景的虚拟相机位置与近景淡出适配。
- DH 和 Voxy 的外部缓存均使用自己的原生格式；跨游戏版本时推荐使用 `copy`，以隔离后端可能执行的格式迁移。

## 1.0.0 - 2026-08-14

### 新增

- 项目从旧 Stage Dimensions 原型迁移为 Architectury `common`、Fabric 和 Forge 多模块工程，目标运行环境为 Minecraft 1.20.1 / Java 17。
- 新增隔离关卡维度与区域槽位。不同关卡实例使用分离区域，同一实例可容纳一个或多个玩家。
- 玩家离开或异常恢复时返回原维度、坐标与朝向；实例 UUID 写入 `DynamicStageInstance` 持久化 NBT，供 KubeJS 等外部系统识别。
- 新增六面无方块边界、染色网格渲染和服务端平滑碰撞。
- 新增可配置的主世界天空、末地天空和关闭天空模式。
- 新增 CMDCam 场景导入、客户端 Flight 采样，以及位置、视角和缩放到背景的相对动画映射。
- 新增 DH 与 Voxy LOD 包注册、验证、挂载和退出释放流程；LOD 来源锚点与关卡物理坐标完全解耦。
- Forge 提供独立 DH 与 Connector/Voxy 开发客户端，避免两个 LOD 后端在同一运行环境中互相冲突。

### 架构变更

- DS 不再烘焙自有 LOD、不读取原生数据库生成中间网格，也不负责 LOD 绘制；DH/Voxy 继续管理其缓存、网格构建和原生渲染。
- 服务端只管理实例、边界、模板状态、LOD 包 ID/锚点和 Flight 时间等小型同步数据，正常运行时不承担 LOD 生成或逐帧动画同步。
- 删除旧 `.sdb`/Backdrop Blob、服务端原生数据库读取器、自有下载缓存/渲染器，以及 RocksDB、SQLite、Zstd、LZ4、XZ 等旧链路依赖。

### 修复

- DH/Voxy 虚拟相机按“来源锚点相对关卡原点”映射，修复背景错误绑定关卡绝对坐标的问题。
- 离开关卡时精确释放 Voxy 存储和 DH wrapper，恢复来源世界后端状态。
- 修复生产包 Mixin refmap 与平台资源元数据，避免开发环境正常但整合包启动失败。

## 当前架构与升级注意事项

- DS 只负责选择 LOD 包、计算虚拟来源位置、调度 Flight 和合成转场，最终 LOD 始终由 DH 或 Voxy 原生渲染器绘制。
- DH 与 Voxy 通常二选一。同一客户端当前一次只挂载一个原生 LOD 背景。
- Voxy 包以 `lodPackId + baseDirectory + worldId` 隔离。不同包可保留相同来源 `worldId`，其 RocksDB 不会因此自动合并。
- 自动使用当前世界 LOD 时，会在 `dynamicstage/lodpacks/dynamicstage/auto/` 创建持久化链接包，但默认不复制源缓存。移动或删除来源存档后，该链接会失效。
- 需要跨实例或随整合包/服务器分发的背景应使用 `import copy`，或由 Voxy 优化器生成新的自包含包，再部署到 `dynamicstage/lodpacks/<namespace>/<pack>/`。
- 现有同名本地包会优先被复用，服务端不会静默覆盖它。更新已分发 LOD 时应使用带版本号的新包 ID。
- DH/Voxy 打开缓存时仍可能创建锁或执行自身格式迁移。跨 Minecraft 或后端版本使用缓存前应保留原始备份，并优先在独立副本上测试。
- 模板会保存方块、方块实体和一般实体，但无法自动捕获其他模组只存于全局 `SavedData` 的区域状态；此类内容需要专用兼容适配器。
- 光影包若替换原版天空、云层或影子相机，其自定义渲染不会自动跟随 Flight。关卡方块、实体、粒子和玩家主镜头也不会被背景 Flight 移动。
- 音乐播放继续由 `mob-battle-music` 等外部模组负责；DS 提供实例 NBT、命令和同步 Flight 时间供 KubeJS 编排。
